import React from 'react';

const Intro = () => {

    // any
    let hero: string;
    function getHero(): string{
        return "true";
    }
    hero = getHero();

    return (
        <div>
            intro
        </div>
    );
};

export default Intro;